# Config Manager - Variables de Entorno
# Copia este archivo a .env y ajusta los valores según tus necesidades

# Directorio home del usuario (por defecto usa $HOME)
# Si está vacío, se usará $HOME automáticamente
USER_HOME=

# Directorio donde se encuentran las configuraciones (por defecto: .config)
CONFIG_DIR=.config

# Lista de aplicaciones/configuraciones a gestionar
# Separadas por espacios o comas
# Formato: nombre_app (se buscará en $USER_HOME/$CONFIG_DIR/nombre_app)
# Nota: 'zsh' y 'dolphin' tienen manejo especial (no van en .config)
CONFIG_APPS=hypr, wofi, wleave, kitty, waybar, mako, eww, gtk-3.0, gtk-4.0, zsh

# Número máximo de backups a mantener (los más antiguos se eliminan automáticamente)
MAX_BACKUPS=5

